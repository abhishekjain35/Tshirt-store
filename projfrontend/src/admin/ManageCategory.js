import React from "react";
import Base from "../core/Base";

const ManageCategory = () => {
    return (
        <Base>
            <h1 className="text-white">Assign</h1>
        </Base>
    );
};

export default ManageCategory;
